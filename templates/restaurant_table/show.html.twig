{% extends 'base.html.twig' %}

{% block title %}RestaurantTable{% endblock %}

{% block body %}
    <h1>RestaurantTable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant_table.id }}</td>
            </tr>
            <tr>
                <th>NumeroTable</th>
                <td>{{ restaurant_table.numeroTable }}</td>
            </tr>
            <tr>
                <th>Capacité</th>
                <td>{{ restaurant_table.capacité }}</td>
            </tr>
            <tr>
                <th>Emplacement</th>
                <td>{{ restaurant_table.emplacement }}</td>
            </tr>
            <tr>
                <th>état</th>
                <td>{{ restaurant_table.état }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restaurant_table_index') }}">back to list</a>

    <a href="{{ path('app_restaurant_table_edit', {'id': restaurant_table.id}) }}">edit</a>

    {{ include('restaurant_table/_delete_form.html.twig') }}
{% endblock %}
